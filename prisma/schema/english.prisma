model english_words {
  id             Int              @id @default(autoincrement())
  word           String           @unique
  created_at     DateTime         @default(now())
  updated_at     DateTime?
  jp_en_meanings jp_en_meanings[]
}
